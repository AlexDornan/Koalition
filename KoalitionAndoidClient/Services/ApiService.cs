using System;
using System.Collections.Generic;
using System.Linq;
using System.Net.Http.Json;
using System.Text;
using System.Threading.Tasks;
//using KoalitionServer.Models;

namespace KoalitionAndoidClient.Services
{
    /*public async Task<User> RegisterUser(RegistrationRequest regRequest)
    {
        var client = new HttpClient();
        var response = await client.PostAsJsonAsync("https://localhost:7127/register", regRequest);
        response.EnsureSuccessStatusCode();
        var user = await response.Content.ReadFromJsonAsync<User>();
        return user;
    }*/
}
