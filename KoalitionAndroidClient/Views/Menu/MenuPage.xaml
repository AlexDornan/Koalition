<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="KoalitionAndroidClient.Views.Menu.MenuPage"
             Title="MenuPage">
    <StackLayout Padding="10">
        <Label FontSize="18" Text="Welcome to Koalition" HorizontalOptions="Center"/>

        <Button Text="Create Chat" Command="{Binding EnterCreateGroupChatCommand}" HorizontalOptions="Center" />

        <CollectionView ItemsSource="{Binding GroupChats}" x:Name="collectionView">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <StackLayout Padding="10">
                        <HorizontalStackLayout>
                            <Button
                        Text="{Binding Name}"
                        Command="{Binding BindingContext.EnterChatCommand, Source={x:Reference collectionView}}"
                        CommandParameter="{Binding .}"
                        HorizontalOptions="Center" />
                        </HorizontalStackLayout>
                    </StackLayout>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </StackLayout>
</ContentPage>
